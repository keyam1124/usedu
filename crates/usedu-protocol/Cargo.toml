[package]
name = "usedu-protocol"
version = "0.1.0"
edition = "2021"
description = "Versioned machine-readable protocol DTOs for usedu."
license = "MIT"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
usedu-core = { path = "../usedu-core" }
